services:

  cyberguard:

    build: .

    container_name: cyberguard-ai

    ports:
      - "8501:8501"

    volumes:
      - ./data:/app/data

    env_file:
      - .env

    restart: unless-stopped